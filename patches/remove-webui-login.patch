diff --git a/packages/desktop/src/renderer/components/layout/Sider/index.tsx b/packages/desktop/src/renderer/components/layout/Sider/index.tsx
index 5cabf3500..61d2d5570 100644
--- a/packages/desktop/src/renderer/components/layout/Sider/index.tsx
+++ b/packages/desktop/src/renderer/components/layout/Sider/index.tsx
@@ -28,13 +28,16 @@ const Sider: React.FC<SiderProps> = ({ onSessionClick, collapsed = false }) => {
 
   const navigate = useNavigate();
   const { closePreview, clearPreviewForScope } = usePreviewContext();
-  const { logout, status } = useAuth();
+  const { logout, status, user } = useAuth();
   const { theme, setTheme } = useThemeContext();
   const [isBatchMode, setIsBatchMode] = useState(false);
   const isSettings = pathname.startsWith('/settings');
   const lastNonSettingsPathRef = useRef('/guid');
   const showLogout =
-    typeof window !== 'undefined' && !(window as { electronAPI?: unknown }).electronAPI && status === 'authenticated';
+    typeof window !== 'undefined' &&
+    !(window as { electronAPI?: unknown }).electronAPI &&
+    status === 'authenticated' &&
+    user !== null;
 
   useEffect(() => {
     if (!pathname.startsWith('/settings')) {
diff --git a/packages/desktop/src/renderer/hooks/context/AuthContext.tsx b/packages/desktop/src/renderer/hooks/context/AuthContext.tsx
index de9575e78..4a4d9e7e9 100644
--- a/packages/desktop/src/renderer/hooks/context/AuthContext.tsx
+++ b/packages/desktop/src/renderer/hooks/context/AuthContext.tsx
@@ -49,4 +49,5 @@ const AuthContext = createContext<AuthContextValue | undefined>(undefined);
 const AUTH_USER_ENDPOINT = '/api/auth/user';
 
 const isDesktopRuntime = typeof window !== 'undefined' && Boolean(window.electronAPI);
+const COMMUNITY_LOGIN_BYPASS = true;
 
@@ -119,7 +120,7 @@ export const AuthProvider: React.FC<React.PropsWithChildren> = ({ children }) =>
   const abortRef = useRef<AbortController | null>(null);
 
   const refresh = useCallback(async () => {
-    if (isDesktopRuntime) {
+    if (isDesktopRuntime || COMMUNITY_LOGIN_BYPASS) {
       setStatus('authenticated');
       setUser(null);
       setReady(true);
@@ -151,7 +152,7 @@ export const AuthProvider: React.FC<React.PropsWithChildren> = ({ children }) =>
 
   const login = useCallback(async ({ username, password, remember }: LoginParams): Promise<LoginResult> => {
     try {
-      if (isDesktopRuntime) {
+      if (isDesktopRuntime || COMMUNITY_LOGIN_BYPASS) {
         setReady(true);
         return { success: true };
       }
@@ -253,7 +254,7 @@ export const AuthProvider: React.FC<React.PropsWithChildren> = ({ children }) =>
   }, []);
 
   const logout = useCallback(async () => {
-    if (isDesktopRuntime) {
+    if (isDesktopRuntime || COMMUNITY_LOGIN_BYPASS) {
       setUser(null);
       setStatus('authenticated');
       setReady(true);
